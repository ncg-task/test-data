(Contribution||has||Model)
(Model||compute||gradients)
(gradients||based on||approximate global normalization)
(Model||introduce||global normalization)
(global normalization||with||conditional random field ( CRF ) objective)
(global normalization||to overcome||label bias problem)
(Model||use||beam inference)
(beam inference||use||early updates)
(beam inference||has||approximate)
(approximate||has||partition function)
(partition function||by||summing over)
(summing over||has||elements)
(elements||in||beam)
(Model||perform||full backpropagation training)
(full backpropagation training||of||all neural network parameters)
(all neural network parameters||based on||CRF loss)
(Model||perform||beam search)
(beam search||for maintaining||multiple hypotheses)
(Model||uses||transition system and feature embeddings)
(Model||demonstrate||simple feed - forward networks)
(simple feed - forward networks||achieve||comparable or better accuracies)
(comparable or better accuracies||than||LSTMs)
(simple feed - forward networks||has||globally normalized)
(simple feed - forward networks||without||any recurrence)
